info:
  name: Get Account
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/api/accounts/08ff975b-7551-48dc-bb0b-c531293fcc3b"
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
