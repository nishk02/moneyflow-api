info:
  name: Update Account
  type: http
  seq: 3

http:
  method: PUT
  url: "{{BASE_URL}}/api/accounts/6f31f86c-4dc6-4d31-9ff7-e61341455d79"
  body:
    type: json
    data: |-
      {
        "currentBalance": 37500.00
      }
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
