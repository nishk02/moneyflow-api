info:
  name: Delete Account
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{BASE_URL}}/api/accounts/a38dffdb-81ec-440b-9d03-5be326643f8c"
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
