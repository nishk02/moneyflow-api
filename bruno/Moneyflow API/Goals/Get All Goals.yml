info:
  name: Get All Goals
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/goals"
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
