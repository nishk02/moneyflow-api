info:
  name: Get Available Periods
  type: http
  seq: 10

http:
  method: GET
  url: "{{BASE_URL}}/api/transactions/available-periods"
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
