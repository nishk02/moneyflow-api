info:
  name: Get Transactions - Calendar
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/transactions?calendarYear=2026&calendarMonth=9&sort=date,desc"
  params:
    - name: calendarYear
      value: "2026"
      type: query
    - name: calendarMonth
      value: "9"
      type: query
    - name: sort
      value: date,desc
      type: query
    - name: page
      value: "0"
      type: query
      disabled: true
    - name: size
      value: "10"
      type: query
      disabled: true
    - name: flowType
      value: EXPENSE
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
