info:
  name: Dashboard
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/dashboard"
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
