info:
  name: Cashflow Summary - WEEKLY
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/analytics/cashflow-summary?mode=WEEKLY&anchor=2026-07-01"
  params:
    - name: mode
      value: WEEKLY
      type: query
    - name: anchor
      value: 2026-07-01
      type: query
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
