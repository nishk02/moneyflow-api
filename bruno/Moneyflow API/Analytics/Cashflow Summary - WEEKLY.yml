info:
  name: Cashflow Summary - WEEKLY
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/analytics/cashflow-summary?mode=WEEKLY"
  params:
    - name: mode
      value: WEEKLY
      type: query
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
