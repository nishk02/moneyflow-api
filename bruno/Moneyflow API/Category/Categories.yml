info:
  name: Categories
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/categories"
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
