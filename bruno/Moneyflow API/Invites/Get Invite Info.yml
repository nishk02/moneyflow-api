info:
  name: Get Invite Info
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/auth/invites/{{INVITE_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
